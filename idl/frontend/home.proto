syntax = "proto3";

package frontend.home;

option go_package = "frontend/home";

import "api.proto";
import "frontend/common.proto"

service HomeService {
  rpc Home(Empty) returns(common.Empty) {
    option (api.get) = "/";
  }
}
